# Copy to .env for local sandbox runs. Never commit .env or real tokens.
# See doc/testing.md for the full testing setup.

# Sandbox or production token — the SDK treats both identically.
ENVIATODO_TOKEN=
